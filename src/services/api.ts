// export const BASE_URL = process.env.NEXT_PUBLIC_URL;

// import io from "socket.io-client";

// export function webSocket(symbol: string) {
//   return io(BASE_URL + "/ws/" + symbol + "@bookTicker");
// }
